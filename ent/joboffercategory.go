// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/marianozunino/jobby/ent/category"
	"github.com/marianozunino/jobby/ent/joboffer"
	"github.com/marianozunino/jobby/ent/joboffercategory"
)

// JobOfferCategory is the model entity for the JobOfferCategory schema.
type JobOfferCategory struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// JobOfferID holds the value of the "job_offer_id" field.
	JobOfferID uuid.UUID `json:"job_offer_id,omitempty"`
	// CategoryID holds the value of the "category_id" field.
	CategoryID uuid.UUID `json:"category_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt time.Time `json:"deleted_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the JobOfferCategoryQuery when eager-loading is set.
	Edges        JobOfferCategoryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// JobOfferCategoryEdges holds the relations/edges for other nodes in the graph.
type JobOfferCategoryEdges struct {
	// Category holds the value of the category edge.
	Category *Category `json:"category,omitempty"`
	// JobOffer holds the value of the job_offer edge.
	JobOffer *JobOffer `json:"job_offer,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CategoryOrErr returns the Category value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JobOfferCategoryEdges) CategoryOrErr() (*Category, error) {
	if e.loadedTypes[0] {
		if e.Category == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: category.Label}
		}
		return e.Category, nil
	}
	return nil, &NotLoadedError{edge: "category"}
}

// JobOfferOrErr returns the JobOffer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JobOfferCategoryEdges) JobOfferOrErr() (*JobOffer, error) {
	if e.loadedTypes[1] {
		if e.JobOffer == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: joboffer.Label}
		}
		return e.JobOffer, nil
	}
	return nil, &NotLoadedError{edge: "job_offer"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JobOfferCategory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case joboffercategory.FieldCreatedAt, joboffercategory.FieldUpdatedAt, joboffercategory.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		case joboffercategory.FieldID, joboffercategory.FieldJobOfferID, joboffercategory.FieldCategoryID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JobOfferCategory fields.
func (joc *JobOfferCategory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case joboffercategory.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				joc.ID = *value
			}
		case joboffercategory.FieldJobOfferID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field job_offer_id", values[i])
			} else if value != nil {
				joc.JobOfferID = *value
			}
		case joboffercategory.FieldCategoryID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field category_id", values[i])
			} else if value != nil {
				joc.CategoryID = *value
			}
		case joboffercategory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				joc.CreatedAt = value.Time
			}
		case joboffercategory.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				joc.UpdatedAt = value.Time
			}
		case joboffercategory.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				joc.DeletedAt = value.Time
			}
		default:
			joc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JobOfferCategory.
// This includes values selected through modifiers, order, etc.
func (joc *JobOfferCategory) Value(name string) (ent.Value, error) {
	return joc.selectValues.Get(name)
}

// QueryCategory queries the "category" edge of the JobOfferCategory entity.
func (joc *JobOfferCategory) QueryCategory() *CategoryQuery {
	return NewJobOfferCategoryClient(joc.config).QueryCategory(joc)
}

// QueryJobOffer queries the "job_offer" edge of the JobOfferCategory entity.
func (joc *JobOfferCategory) QueryJobOffer() *JobOfferQuery {
	return NewJobOfferCategoryClient(joc.config).QueryJobOffer(joc)
}

// Update returns a builder for updating this JobOfferCategory.
// Note that you need to call JobOfferCategory.Unwrap() before calling this method if this JobOfferCategory
// was returned from a transaction, and the transaction was committed or rolled back.
func (joc *JobOfferCategory) Update() *JobOfferCategoryUpdateOne {
	return NewJobOfferCategoryClient(joc.config).UpdateOne(joc)
}

// Unwrap unwraps the JobOfferCategory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (joc *JobOfferCategory) Unwrap() *JobOfferCategory {
	_tx, ok := joc.config.driver.(*txDriver)
	if !ok {
		panic("ent: JobOfferCategory is not a transactional entity")
	}
	joc.config.driver = _tx.drv
	return joc
}

// String implements the fmt.Stringer.
func (joc *JobOfferCategory) String() string {
	var builder strings.Builder
	builder.WriteString("JobOfferCategory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", joc.ID))
	builder.WriteString("job_offer_id=")
	builder.WriteString(fmt.Sprintf("%v", joc.JobOfferID))
	builder.WriteString(", ")
	builder.WriteString("category_id=")
	builder.WriteString(fmt.Sprintf("%v", joc.CategoryID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(joc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(joc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(joc.DeletedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// JobOfferCategories is a parsable slice of JobOfferCategory.
type JobOfferCategories []*JobOfferCategory

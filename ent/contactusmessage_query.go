// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/marianozunino/cc-backend-go/ent/contactusmessage"
	"github.com/marianozunino/cc-backend-go/ent/predicate"
)

// ContactUsMessageQuery is the builder for querying ContactUsMessage entities.
type ContactUsMessageQuery struct {
	config
	ctx        *QueryContext
	order      []contactusmessage.OrderOption
	inters     []Interceptor
	predicates []predicate.ContactUsMessage
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ContactUsMessageQuery builder.
func (cumq *ContactUsMessageQuery) Where(ps ...predicate.ContactUsMessage) *ContactUsMessageQuery {
	cumq.predicates = append(cumq.predicates, ps...)
	return cumq
}

// Limit the number of records to be returned by this query.
func (cumq *ContactUsMessageQuery) Limit(limit int) *ContactUsMessageQuery {
	cumq.ctx.Limit = &limit
	return cumq
}

// Offset to start from.
func (cumq *ContactUsMessageQuery) Offset(offset int) *ContactUsMessageQuery {
	cumq.ctx.Offset = &offset
	return cumq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cumq *ContactUsMessageQuery) Unique(unique bool) *ContactUsMessageQuery {
	cumq.ctx.Unique = &unique
	return cumq
}

// Order specifies how the records should be ordered.
func (cumq *ContactUsMessageQuery) Order(o ...contactusmessage.OrderOption) *ContactUsMessageQuery {
	cumq.order = append(cumq.order, o...)
	return cumq
}

// First returns the first ContactUsMessage entity from the query.
// Returns a *NotFoundError when no ContactUsMessage was found.
func (cumq *ContactUsMessageQuery) First(ctx context.Context) (*ContactUsMessage, error) {
	nodes, err := cumq.Limit(1).All(setContextOp(ctx, cumq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{contactusmessage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) FirstX(ctx context.Context) *ContactUsMessage {
	node, err := cumq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ContactUsMessage ID from the query.
// Returns a *NotFoundError when no ContactUsMessage ID was found.
func (cumq *ContactUsMessageQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = cumq.Limit(1).IDs(setContextOp(ctx, cumq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{contactusmessage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := cumq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ContactUsMessage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ContactUsMessage entity is found.
// Returns a *NotFoundError when no ContactUsMessage entities are found.
func (cumq *ContactUsMessageQuery) Only(ctx context.Context) (*ContactUsMessage, error) {
	nodes, err := cumq.Limit(2).All(setContextOp(ctx, cumq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{contactusmessage.Label}
	default:
		return nil, &NotSingularError{contactusmessage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) OnlyX(ctx context.Context) *ContactUsMessage {
	node, err := cumq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ContactUsMessage ID in the query.
// Returns a *NotSingularError when more than one ContactUsMessage ID is found.
// Returns a *NotFoundError when no entities are found.
func (cumq *ContactUsMessageQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = cumq.Limit(2).IDs(setContextOp(ctx, cumq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{contactusmessage.Label}
	default:
		err = &NotSingularError{contactusmessage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := cumq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ContactUsMessages.
func (cumq *ContactUsMessageQuery) All(ctx context.Context) ([]*ContactUsMessage, error) {
	ctx = setContextOp(ctx, cumq.ctx, "All")
	if err := cumq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ContactUsMessage, *ContactUsMessageQuery]()
	return withInterceptors[[]*ContactUsMessage](ctx, cumq, qr, cumq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) AllX(ctx context.Context) []*ContactUsMessage {
	nodes, err := cumq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ContactUsMessage IDs.
func (cumq *ContactUsMessageQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if cumq.ctx.Unique == nil && cumq.path != nil {
		cumq.Unique(true)
	}
	ctx = setContextOp(ctx, cumq.ctx, "IDs")
	if err = cumq.Select(contactusmessage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := cumq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cumq *ContactUsMessageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cumq.ctx, "Count")
	if err := cumq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cumq, querierCount[*ContactUsMessageQuery](), cumq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) CountX(ctx context.Context) int {
	count, err := cumq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cumq *ContactUsMessageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cumq.ctx, "Exist")
	switch _, err := cumq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cumq *ContactUsMessageQuery) ExistX(ctx context.Context) bool {
	exist, err := cumq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ContactUsMessageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cumq *ContactUsMessageQuery) Clone() *ContactUsMessageQuery {
	if cumq == nil {
		return nil
	}
	return &ContactUsMessageQuery{
		config:     cumq.config,
		ctx:        cumq.ctx.Clone(),
		order:      append([]contactusmessage.OrderOption{}, cumq.order...),
		inters:     append([]Interceptor{}, cumq.inters...),
		predicates: append([]predicate.ContactUsMessage{}, cumq.predicates...),
		// clone intermediate query.
		sql:  cumq.sql.Clone(),
		path: cumq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ContactUsMessage.Query().
//		GroupBy(contactusmessage.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cumq *ContactUsMessageQuery) GroupBy(field string, fields ...string) *ContactUsMessageGroupBy {
	cumq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ContactUsMessageGroupBy{build: cumq}
	grbuild.flds = &cumq.ctx.Fields
	grbuild.label = contactusmessage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ContactUsMessage.Query().
//		Select(contactusmessage.FieldName).
//		Scan(ctx, &v)
func (cumq *ContactUsMessageQuery) Select(fields ...string) *ContactUsMessageSelect {
	cumq.ctx.Fields = append(cumq.ctx.Fields, fields...)
	sbuild := &ContactUsMessageSelect{ContactUsMessageQuery: cumq}
	sbuild.label = contactusmessage.Label
	sbuild.flds, sbuild.scan = &cumq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ContactUsMessageSelect configured with the given aggregations.
func (cumq *ContactUsMessageQuery) Aggregate(fns ...AggregateFunc) *ContactUsMessageSelect {
	return cumq.Select().Aggregate(fns...)
}

func (cumq *ContactUsMessageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cumq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cumq); err != nil {
				return err
			}
		}
	}
	for _, f := range cumq.ctx.Fields {
		if !contactusmessage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cumq.path != nil {
		prev, err := cumq.path(ctx)
		if err != nil {
			return err
		}
		cumq.sql = prev
	}
	return nil
}

func (cumq *ContactUsMessageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ContactUsMessage, error) {
	var (
		nodes = []*ContactUsMessage{}
		_spec = cumq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ContactUsMessage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ContactUsMessage{config: cumq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cumq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cumq *ContactUsMessageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cumq.querySpec()
	_spec.Node.Columns = cumq.ctx.Fields
	if len(cumq.ctx.Fields) > 0 {
		_spec.Unique = cumq.ctx.Unique != nil && *cumq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cumq.driver, _spec)
}

func (cumq *ContactUsMessageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(contactusmessage.Table, contactusmessage.Columns, sqlgraph.NewFieldSpec(contactusmessage.FieldID, field.TypeUUID))
	_spec.From = cumq.sql
	if unique := cumq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cumq.path != nil {
		_spec.Unique = true
	}
	if fields := cumq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, contactusmessage.FieldID)
		for i := range fields {
			if fields[i] != contactusmessage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cumq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cumq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cumq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cumq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cumq *ContactUsMessageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cumq.driver.Dialect())
	t1 := builder.Table(contactusmessage.Table)
	columns := cumq.ctx.Fields
	if len(columns) == 0 {
		columns = contactusmessage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cumq.sql != nil {
		selector = cumq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cumq.ctx.Unique != nil && *cumq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cumq.predicates {
		p(selector)
	}
	for _, p := range cumq.order {
		p(selector)
	}
	if offset := cumq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cumq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ContactUsMessageGroupBy is the group-by builder for ContactUsMessage entities.
type ContactUsMessageGroupBy struct {
	selector
	build *ContactUsMessageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cumgb *ContactUsMessageGroupBy) Aggregate(fns ...AggregateFunc) *ContactUsMessageGroupBy {
	cumgb.fns = append(cumgb.fns, fns...)
	return cumgb
}

// Scan applies the selector query and scans the result into the given value.
func (cumgb *ContactUsMessageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cumgb.build.ctx, "GroupBy")
	if err := cumgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContactUsMessageQuery, *ContactUsMessageGroupBy](ctx, cumgb.build, cumgb, cumgb.build.inters, v)
}

func (cumgb *ContactUsMessageGroupBy) sqlScan(ctx context.Context, root *ContactUsMessageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cumgb.fns))
	for _, fn := range cumgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cumgb.flds)+len(cumgb.fns))
		for _, f := range *cumgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cumgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cumgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ContactUsMessageSelect is the builder for selecting fields of ContactUsMessage entities.
type ContactUsMessageSelect struct {
	*ContactUsMessageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cums *ContactUsMessageSelect) Aggregate(fns ...AggregateFunc) *ContactUsMessageSelect {
	cums.fns = append(cums.fns, fns...)
	return cums
}

// Scan applies the selector query and scans the result into the given value.
func (cums *ContactUsMessageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cums.ctx, "Select")
	if err := cums.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContactUsMessageQuery, *ContactUsMessageSelect](ctx, cums.ContactUsMessageQuery, cums, cums.inters, v)
}

func (cums *ContactUsMessageSelect) sqlScan(ctx context.Context, root *ContactUsMessageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cums.fns))
	for _, fn := range cums.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cums.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cums.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
